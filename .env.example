QBITTORRENT_URL=http://qbittorrent:8080
QBITTORRENT_USERNAME=admin
QBITTORRENT_PASSWORD=change-me
WATCH_PATHS=/data
EXCLUDED_GLOBS=/data/logs/*,/data/orphaned-files/*,find_duplicates.sh,rar_extractor.sh,video_folders.log
DRY_RUN=true
POLL_INTERVAL_SECONDS=300
ORPHAN_GRACE_SECONDS=21600
MIN_FILE_AGE_SECONDS=1800
DELETE_EMPTY_DIRS=true
PROTECT_SINGLE_FILE_PARENT_DIRS=true
STATE_DB_PATH=/config/state.sqlite3
REPORT_PATH=/config/last-run.json
LOG_LEVEL=INFO

# Attempts per idempotent (GET/HEAD) request to qBittorrent/Plex/Radarr/Sonarr.
# 1 = no retry (default). Raise it (e.g. 3) so a transient 5xx or connection blip
# on a LAN call is retried with exponential backoff instead of failing the
# read-only report on the first hiccup. qBittorrent's login POST is never retried.
HTTP_MAX_ATTEMPTS=1

# RAR extraction (opt-in) — runs in every `scan`/`service` cycle (and the
# standalone `extract` subcommand). Detects and extracts RAR archives in the
# download path so Radarr/Sonarr can import scene releases, then protects the
# extracted media from the deletion pass. Off by default and honors DRY_RUN
# (dry-run reports would-extract and writes nothing). Linux-container only (needs
# the bundled `unar` binary). Set EXTRACT_OWNER to a numeric uid:gid
# (Unraid = 99:100) for a best-effort chown of extracted files; leave empty to
# skip. EXTRACT_PROTECT_SECONDS is how long extracted output stays undeletable
# after extraction (default 24h) so *arr can import before cleanup resumes.
EXTRACT_ENABLED=false
EXTRACT_TOOL=unar
EXTRACT_OWNER=
EXTRACT_MIN_AGE_SECONDS=300
EXTRACT_PROTECT_SECONDS=86400

# Plex duplicate report — consumed by the read-only `plex-duplicates` subcommand.
# Unused by the scan/service cleanup commands; leave PLEX_URL/PLEX_TOKEN empty if
# you don't run the duplicate report.
PLEX_URL=
PLEX_TOKEN=
PLEX_SECTIONS=
PLEX_TIMEOUT_SECONDS=30
PLEX_VERIFY_TLS=true
PLEX_DUPLICATE_REPORT_PATH=/config/plex-duplicates.json

# Optional content-hash confirmation pass for the plex-duplicates report.
#   off     — default; no media is read, report is size-only (unchanged).
#   partial — hash size + first/last 4 MiB per copy (constant cost, strong signal).
#   full    — whole-file sha256 (byte-for-byte "hash-confirmed").
# Confirms 'identical' groups are truly byte-identical and downgrades any that are
# not to 'different-content' (never reclaimable). Reads Plex paths via
# WEB_MEDIA_PATH_MAP (below), so that map must be set and the media mounted
# read-only. Run it where the media lives (the Unraid container).
HASH_MODE=off

# Optional Radarr/Sonarr tracking for the plex-duplicates report. Each *arr is
# enabled only when both its URL and API key are set; it annotates whether a
# duplicate copy is tracked (deleting it re-downloads) vs safe. Read-only.
RADARR_URL=
RADARR_API_KEY=
RADARR_TIMEOUT_SECONDS=30
RADARR_VERIFY_TLS=true
SONARR_URL=
SONARR_API_KEY=
SONARR_TIMEOUT_SECONDS=30
SONARR_VERIFY_TLS=true

# Web GUI for the Plex duplicate report. Run it standalone with the `web`
# subcommand, or set WEB_ENABLED=true to also serve it from the long-running
# `service` (one container that both cleans up and serves the report). It reads
# the on-disk PLEX_DUPLICATE_REPORT_PATH snapshot and renders a browsable page
# plus a JSON API at /api/report — it never runs a scan. Read-only by default;
# binds to all interfaces so a mapped container port is reachable; LAN-scoped like
# the rest of the stack.
WEB_ENABLED=false
WEB_BIND_ADDRESS=0.0.0.0
WEB_PORT=8080

# Web action layer (Phase 2): let an operator delete redundant copies from the
# browser. This is the first outside-triggered deletion of *library* media, so it
# is fail-closed. Off by default; even when enabled, WEB_ACTIONS_DRY_RUN reports
# what would be deleted and touches nothing until you set it false. The browser
# reclaim is two-step (a confirmation page previews exactly what would be deleted
# before anything is removed). Every reclaim must present WEB_ACTION_TOKEN — via the
# X-Action-Token header for the JSON API, or pasted once in the browser to set an
# HttpOnly/SameSite unlock cookie (so the confirm submit is not re-pasted). The
# confirm page also carries the same signed unlock value as a hidden field, so the
# two-step flow works with cookies disabled (no secret in the page). Enabling actions
# without a token refuses every request. WEB_ACTION_SESSION_SECONDS sets the unlock
# lifetime (cookie Max-Age + signed-token expiry); non-positive falls back to 3600.
# WEB_MEDIA_PATH_MAP maps Plex-reported paths to this container's mounts (comma-
# separated plex_prefix:container_prefix pairs) so a filesystem delete of an
# *untracked* copy can find the file; an unmapped path is refused. Tracked copies are
# deleted via Radarr/Sonarr (so they don't re-download) and need no path map.
WEB_ENABLE_ACTIONS=false
WEB_ACTIONS_DRY_RUN=true
WEB_ACTION_TOKEN=
WEB_ACTION_SESSION_SECONDS=3600
WEB_MEDIA_PATH_MAP=

# CSRF/origin hardening for a non-loopback bind. On the default loopback bind the
# behavior is unchanged; when WEB_BIND_ADDRESS is not loopback (e.g. 0.0.0.0), a
# browser reclaim FORM must present a same-origin Origin (or Referer) — a cross-site
# form POST is refused even if it omits Origin. The JSON API stays token-only when
# it sends no Origin (so curl keeps working). Behind a TLS-terminating reverse proxy
# the server sees plain HTTP and cannot trust the client Host to infer the external
# scheme, so set WEB_ALLOWED_ORIGINS to the external origin(s) the browser sends —
# a comma-separated list of full origins, e.g. https://media.example.com. Leave
# empty for a direct LAN bind (the same-origin-vs-Host check is used instead).
WEB_ALLOWED_ORIGINS=

# DNS-rebinding defense: every route refuses a request whose Host header is an
# unrecognized hostname, before it routes. IP-literal and localhost hosts are always
# accepted (an IP can't be rebound), so direct LAN access by IP needs no config. If
# you reach the UI through a hostname (e.g. tower.local) list it here, comma-separated
# — the hostnames of WEB_ALLOWED_ORIGINS are added automatically. X-Forwarded-* is
# never trusted for this.
WEB_ALLOWED_HOSTS=

# Optionally gate the read-only reclaim action-history views (/actions + /api/actions,
# which expose previously-deleted absolute paths + sizes) behind the reclaim credential.
# Off (the default) keeps them LAN-readable like the report. When true, /api/actions
# needs the X-Action-Token header (or the unlock cookie) and /actions needs the browser
# unlock session earned by presenting WEB_ACTION_TOKEN on the report page. It
# authenticates against the token, so it needs actions enabled AND a token set —
# otherwise it fails closed (history denied to everyone; a startup warning notes the
# lockout). The report views (/ and /api/report) are never affected.
WEB_ACTION_HISTORY_AUTH=false

# Optionally gate the REPORT surface (/ + /index.html + /api/report, which expose media
# paths + sizes) behind the reclaim credential. Off (the default) keeps it LAN-readable.
# When true, /api/report needs the X-Action-Token header and the browser routes need the
# unlock session; because / is the page you normally unlock through, its gated 403 page
# carries a no-JS token form (POST /actions/unlock) that mints the session and redirects
# back. Independent of WEB_ACTION_HISTORY_AUTH — set BOTH to gate the entire read surface.
# Fails closed (report denied to everyone; startup warning) when actions are off or no
# token is set.
WEB_ACTION_REPORT_AUTH=false

# Optionally add a single self-contained inline enhancement script to the reclaim form
# (select-all + a live selected count/size). Off (the default) keeps the strict no-script
# CSP. When true (and actions are enabled) the script is admitted by a fresh per-response
# script-src 'nonce-…' — no external asset, no fetch/XHR — and the form works fully with
# JavaScript disabled.
WEB_ACTION_INLINE_SCRIPT=false
